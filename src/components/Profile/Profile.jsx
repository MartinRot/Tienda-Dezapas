import React from 'react'
import { IconButton } from '@mui/material'
import PersonIcon from '@mui/icons-material/Person';

const Profile = () => {
    return (
            
        <IconButton>
            <PersonIcon sx={{color:'rgba(0,0,0,.7)'}}/>
        </IconButton>        

    )
}

export default Profile
